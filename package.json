{
  "name": "el-bruto",
  "version": "0.2.2",
  "private": true,
  "description": "Phase A CLI automation for EternalTwin La Brute arena fights.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "dev": "tsx src/main.ts",
    "test": "node --test dist/test/**/*.test.js",
    "check": "npm run build && npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
